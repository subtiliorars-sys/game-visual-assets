# Textures moved / mirrored

Full **go-big** CC0 texture vault lives in:

**https://github.com/subtiliorars-sys/game-texture-assets**

- `vendor/ambientcg/` — ~1585 ambientCG 1K materials  
- `vendor/polyhaven/` — **780** Poly Haven 1K textures (complete catalog)  
- Release zips: https://github.com/subtiliorars-sys/game-texture-assets/releases/tag/gobig-2026-07-13  

On this machine the local cache also remains under this repo’s `vendor/ambientcg` + `vendor/polyhaven` if present.

Re-fetch: `game-texture-assets/scripts/fetch-*-parallel.ps1`
